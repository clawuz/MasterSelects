# MASterSelects Environment Variables
# Copy this file to .env and fill in the values

# Currently no environment variables required.
# Vite's built-in import.meta.env.DEV is used for development mode detection.
