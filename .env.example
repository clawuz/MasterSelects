# MASterSelects Environment Variables
# Copy this file to .env and fill in the values

# Groq API key — seeded into IndexedDB on first launch so users don't need to enter it manually.
# Also set this in Cloudflare Pages > Settings > Environment Variables for production.
# VITE_GROQ_API_KEY=gsk_...
